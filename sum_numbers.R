# Define the function to sum two numbers
sum_two_numbers <- function(a, b) {
  # Add the two numbers
  result <- a + b
  # Return the result
  return(result)
}

# Example usage
# sum_two_numbers(3, 5)